apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: ssh-honeypot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: "honeypot"
            - name: POSTGRES_USER
              value: "honeypot"
            - name: POSTGRES_PASSWORD
              value: "honeypot"